module.exports = function(app){
    const alert = require('../controllers/alertController');
    const jwtMiddleware = require('../../../config/jwtMiddleware');

    
    //app.post('/schedules/alerts',jwtMiddleware,alert.postalert);
};
 